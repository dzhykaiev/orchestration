/**
 * @deprecated Use `lib/companyNavigation` directly.
 * Backward-compatibility re-export for legacy imports.
 */
export {
  buildBoardHref,
  buildCompanyHref,
  buildWorkspaceHref,
  resolveCompanySelection,
  resolveWorkspaceSelection,
} from "./companyNavigation";
